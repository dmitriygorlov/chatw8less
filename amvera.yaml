meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"

build:
  requirementsPath: requirements.txt
  useCache: true

run:
  command: uvicorn --host 0.0.0.0 --port 8000 web_app:app
  persistenceMount: /data
  containerPort: 8000
